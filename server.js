const http=require('http');const fs=require('fs');const path=require('path');const crypto=require('crypto');
const PORT=Number(process.env.PORT||3000),KEY=process.env.TIKTOK_CLIENT_KEY||'',SECRET=process.env.TIKTOK_CLIENT_SECRET||'',REDIRECT_ENV=process.env.TIKTOK_REDIRECT_URI||'',SCOPES=process.env.TIKTOK_SCOPES||'user.info.basic',VOICE_PROVIDER=process.env.VOICE_PROVIDER||(process.env.ELEVENLABS_API_KEY?'elevenlabs':''),VOICE_API_KEY=process.env.VOICE_API_KEY||process.env.ELEVENLABS_API_KEY||'',VOICE_ID=process.env.VOICE_ID||process.env.ELEVENLABS_VOICE_ID||'',VOICE_CONFIGURED=!!(VOICE_PROVIDER&&VOICE_API_KEY&&VOICE_ID),OPENAI_KEY=process.env.OPENAI_API_KEY||'',OPENAI_MODEL=process.env.OPENAI_MODEL||'gpt-5.6',AI_CONFIGURED=!!OPENAI_KEY,LIVEAVATAR_KEY=process.env.LIVEAVATAR_API_KEY||'',LIVEAVATAR_AVATAR_ID=process.env.LIVEAVATAR_AVATAR_ID||'',LIVEAVATAR_IS_SANDBOX=String(process.env.LIVEAVATAR_IS_SANDBOX||'false').toLowerCase()==='true',LIVEAVATAR_CONFIGURED=!!(LIVEAVATAR_KEY&&LIVEAVATAR_AVATAR_ID);
const sessions=new Map(),aiSessions=new Map(),webhookEvents=new Map(),saraQueues=new Map(),audioStore=new Map();
const liveClients=new Set(),eventLog=[];const MAX_EVENT_LOG=Number(process.env.SARA_MAX_EVENT_LOG||200);
function emitLiveEvent(event){const payload=JSON.stringify({...event,created:event.created||Date.now()});try{eventLog.push(JSON.parse(payload))}catch{}if(eventLog.length>MAX_EVENT_LOG)eventLog.shift();for(const client of liveClients){try{client.write('data: '+payload+'\\n\\n')}catch{liveClients.delete(client)}}}
function safeText(v,max=1200){return String(v??'').trim().slice(0,max)}
function viewerKey(x){return safeText(x.viewerId||x.openId||x.userId||'anonymous',160)}
function stopSara(id){const q=getQueue(id);q.stopped=true;emitLiveEvent({type:'control',action:'stop',sessionId:id})}
function resumeSara(id){const q=getQueue(id);q.stopped=false;emitLiveEvent({type:'control',action:'resume',sessionId:id})}
const MAX_QUEUE=Number(process.env.SARA_MAX_QUEUE||20),AUDIO_TTL=Number(process.env.SARA_AUDIO_TTL_MS||600000),MESSAGE_COOLDOWN=Number(process.env.SARA_MESSAGE_COOLDOWN_MS||1200);
const send=(r,s,t,b)=>{r.writeHead(s,{'Content-Type':t});r.end(b)},json=(r,s,b)=>send(r,s,'application/json',JSON.stringify(b));
function sid(r){const m=(r.headers.cookie||'').match(/(?:^|; )sid=([^;]+)/);return m&&m[1]}function session(r){const id=sid(r);return id&&sessions.get(id)}
function aisid(r,res){const m=(r.headers.cookie||'').match(/(?:^|; )ai_sid=([^;]+)/);if(m)return m[1];const id=crypto.randomBytes(24).toString('hex');res.setHeader('Set-Cookie',`ai_sid=${id}; HttpOnly; SameSite=Lax; ${String(req.headers['x-forwarded-proto']||'').split(',')[0].trim()==='https'?'Secure; ':''}Path=/; Max-Age=86400`);return id}
function tokenCookie(res,id,secure=false){res.setHeader('Set-Cookie',`sid=${id}; HttpOnly; SameSite=Lax; ${secure?'Secure; ':''}Path=/; Max-Age=604800`)}
function tiktokRedirect(req){if(REDIRECT_ENV)return REDIRECT_ENV;const proto=String(req.headers['x-forwarded-proto']||'').split(',')[0].trim()||'http';return `${proto}://${req.headers.host}/auth/tiktok/callback`}\nfunction oauthState(){const nonce=crypto.randomBytes(24).toString('base64url');const sig=crypto.createHmac('sha256',SECRET).update(nonce).digest('base64url');return `${nonce}.${sig}`}\nfunction validOAuthState(state){if(!state||!SECRET)return false;const parts=String(state).split('.');if(parts.length!==2)return false;const expected=crypto.createHmac('sha256',SECRET).update(parts[0]).digest('base64url');try{return crypto.timingSafeEqual(Buffer.from(parts[1]),Buffer.from(expected))}catch{return false}}
async function refreshSession(s){if(!s?.tiktok?.refresh_token)return false;const form=new URLSearchParams({client_key:KEY,client_secret:SECRET,grant_type:'refresh_token',refresh_token:s.tiktok.refresh_token});const rr=await fetch('https://open.tiktokapis.com/v2/oauth/token/',{method:'POST',headers:{'Content-Type':'application/x-www-form-urlencoded','Cache-Control':'no-cache'},body:form});const d=await rr.json();if(!rr.ok||d.error)throw Error(d.error_description||d.error||'TikTok token refresh failed');s.tiktok={...s.tiktok,...d,access_token_expires_at:Date.now()+Number(d.expires_in||86400)*1000,refresh_token_expires_at:Date.now()+Number(d.refresh_expires_in||31536000)*1000};return true}
async function accessToken(s){if(!s?.tiktok?.access_token)throw Error('TikTok account is not connected');const expires=Number(s.tiktok.access_token_expires_at||0);if(expires&&expires-Date.now()>15*60*1000)return s.tiktok.access_token;await refreshSession(s);return s.tiktok.access_token}
async function askAI(history,language){const instructions=`You are SARA, a warm Pakistani female AI virtual host. You are an AI and must never claim to be human. Reply ONLY in Urdu. You may understand Roman Urdu, Urdu script, Punjabi and Pakistani English, but every spoken and written reply must be Urdu. Keep public livestream replies short, friendly and spontaneous, usually 1-3 sentences. If asked whether you are real, clearly say you are an AI virtual host. Never pressure viewers to send gifts or spend money. Never invent real-time TikTok viewer, gift or battle data.`;const rr=await fetch('https://api.openai.com/v1/responses',{method:'POST',headers:{Authorization:`Bearer ${OPENAI_KEY}`,'Content-Type':'application/json'},body:JSON.stringify({model:OPENAI_MODEL,instructions,input:history,max_output_tokens:180})});const d=await rr.json();if(!rr.ok)throw Error(d.error?.message||'OpenAI request failed');return String(d.output_text||'').trim()}
async function makeVoice(text,language){if(!VOICE_CONFIGURED)return null;if(VOICE_PROVIDER!=='elevenlabs')throw Error('Only ElevenLabs is wired in this version.');const lang='urd';const rr=await fetch(`https://api.elevenlabs.io/v1/text-to-speech/${encodeURIComponent(VOICE_ID)}`,{method:'POST',headers:{'xi-api-key':VOICE_API_KEY,'Content-Type':'application/json','Accept':'audio/mpeg'},body:JSON.stringify({text,model_id:'eleven_v3',language_code:lang,voice_settings:{stability:.48,similarity_boost:.8,speed:.98}})});const audio=Buffer.from(await rr.arrayBuffer());if(!rr.ok)throw Error(`ElevenLabs request failed (${rr.status})`);const id=crypto.randomBytes(18).toString('hex');audioStore.set(id,{audio,created:Date.now()});return `/api/sara/audio/${id}`}
async function liveAvatarToken(req){if(!LIVEAVATAR_CONFIGURED)throw Error('LiveAvatar is not configured. Add LIVEAVATAR_API_KEY on the server.');let b='';for await(const c of req)b+=c;let x={};try{x=JSON.parse(b||'{}')}catch{}const avatarId=String(LIVEAVATAR_AVATAR_ID).trim();if(!avatarId)throw Error('LiveAvatar is not configured. Set LIVEAVATAR_AVATAR_ID to a real avatar from your LiveAvatar account.');const body={mode:'FULL',avatar_id:avatarId,is_sandbox:LIVEAVATAR_IS_SANDBOX};const rr=await fetch('https://api.liveavatar.com/v1/sessions/token',{method:'POST',headers:{'X-API-KEY':LIVEAVATAR_KEY,'Content-Type':'application/json'},body:JSON.stringify(body)});const d=await rr.json();if(!rr.ok)throw Error(d?.data?.message||d?.error?.message||'LiveAvatar session token request failed');const token=d?.data?.session_token;if(!token)throw Error('LiveAvatar did not return a session token');return{session_token:token,session_id:d?.data?.session_id||null,avatar_id:avatarId,sandbox:body.is_sandbox}}
function cleanupAudio(){const cutoff=Date.now()-AUDIO_TTL;for(const[k,v]of audioStore)if(v.created<cutoff)audioStore.delete(k)}
function getQueue(id){if(!saraQueues.has(id))saraQueues.set(id,{items:[],active:false,lastAccepted:0,lastResult:null});return saraQueues.get(id)}
async function processQueue(id){const q=getQueue(id);if(q.active||q.stopped)return;q.active=true;while(q.items.length){const item=q.items.shift();if(q.stopped){item.reject(Error('SARA is stopped'));continue}q.lastResult={id:item.id,status:'thinking',text:item.text,created:item.created};emitLiveEvent({type:'sara',stage:'thinking',sessionId:id,messageId:item.id,input:item.text,language:item.language});try{const h=aiSessions.get(id)||[];h.push({role:'user',content:item.text});const reply=await askAI(h.slice(-10),item.language);h.push({role:'assistant',content:reply});aiSessions.set(id,h.slice(-10));q.lastResult={...q.lastResult,status:'speaking',reply};emitLiveEvent({type:'sara',stage:'speaking',sessionId:id,messageId:item.id,reply});try{q.lastResult.audioUrl=await makeVoice(reply,item.language)}catch(e){q.lastResult.voiceError=e.message}q.lastResult.status='ready';emitLiveEvent({type:'sara',stage:'ready',sessionId:id,messageId:item.id,reply,voice:!!q.lastResult.audioUrl});item.resolve(q.lastResult)}catch(e){q.lastResult={...q.lastResult,status:'error',error:e.message};item.reject(e)}}q.active=false;q.lastResult=q.lastResult||null}
async function saraMessage(req,res){if(!AI_CONFIGURED)return json(res,503,{error:'AI is not configured. Add OPENAI_API_KEY.'});const id=aisid(req,res),q=getQueue(id);if(Date.now()-q.lastAccepted<MESSAGE_COOLDOWN)return json(res,429,{error:'SARA is processing too quickly. Please wait a moment.'});let b='';for await(const c of req)b+=c;let x={};try{x=JSON.parse(b)}catch{return json(res,400,{error:'Invalid JSON'})}const text=String(x.text||'').trim().slice(0,1200),language='ur';if(!text)return json(res,400,{error:'text required'});if(q.stopped)return json(res,423,{error:'SARA is stopped. Resume the session before sending messages.'});if(q.items.length>=MAX_QUEUE)return json(res,429,{error:'SARA conversation queue is full. Please try again shortly.'});q.lastAccepted=Date.now();const item={id:crypto.randomBytes(10).toString('hex'),text,language,created:Date.now()};const result=new Promise((resolve,reject)=>{item.resolve=resolve;item.reject=reject;q.items.push(item);processQueue(id)});try{return json(res,200,{ok:true,...await result})}catch(e){return json(res,502,{error:'SARA pipeline failed',detail:e.message})}}
async function tiktok(pathname,method,accessTokenValue,body){const rr=await fetch(`https://open.tiktokapis.com${pathname}`,{method,headers:{Authorization:`Bearer ${accessTokenValue}`,'Content-Type':'application/json; charset=UTF-8'},body:body?JSON.stringify(body):undefined});const d=await rr.json();if(!rr.ok||d.error?.code&&d.error.code!=='ok')throw Error(d.error?.message||`TikTok API request failed (${rr.status})`);return d}
function pruneWebhookEvents(){const cutoff=Date.now()-86400000;for(const[k,v]of webhookEvents)if(v<cutoff)webhookEvents.delete(k)}function webhookId(body){return crypto.createHash('sha256').update(JSON.stringify(body)).digest('hex')}
const server=http.createServer(async(req,res)=>{const u=new URL(req.url,`http://${req.headers.host}`);cleanupAudio();
if(u.pathname==='/api/health')return json(res,200,{ok:true,service:'sara-ai-tiktok-live',tiktokOAuthConfigured:!!(KEY&&SECRET),tiktokScopes:SCOPES.split(',').filter(Boolean),desktopPkce:true,aiConfigured:AI_CONFIGURED,aiModel:AI_CONFIGURED?OPENAI_MODEL:null,voiceConfigured:VOICE_CONFIGURED,voiceProvider:VOICE_PROVIDER||null,liveAvatarConfigured:LIVEAVATAR_CONFIGURED,liveAvatarAvatarConfigured:!!LIVEAVATAR_AVATAR_ID,liveAvatarSandbox:LIVEAVATAR_IS_SANDBOX,tiktokWebhookEndpoint:'/api/tiktok/webhook',liveInteractionApi:false,eventIngestEndpoint:'/api/sara/event',sseEndpoint:'/api/sara/events',emergencyStop:true,saraPipeline:true,queueLimit:MAX_QUEUE});
if(u.pathname==='/api/me'){const s=session(req);return json(res,200,{authenticated:!!s,openId:s?.tiktok?.open_id||null,scope:s?.tiktok?.scope||null,accessTokenExpiresAt:s?.tiktok?.access_token_expires_at||null})}
if(u.pathname==='/api/sara/status'){const id=aisid(req,res),q=getQueue(id);return json(res,200,{ok:true,active:q.active,queued:q.items.length,stopped:!!q.stopped,lastAccepted:q.lastAccepted||null,lastResult:q.lastResult||null,eventLogSize:eventLog.length})}
if(req.method==='GET'&&u.pathname==='/api/sara/events'){res.writeHead(200,{'Content-Type':'text/event-stream','Cache-Control':'no-cache','Connection':'keep-alive','X-Accel-Buffering':'no'});res.write('data: '+JSON.stringify({type:'connected',created:Date.now()})+'\\n\\n');for(const e of eventLog.slice(-20))res.write('data: '+JSON.stringify(e)+'\\n\\n');liveClients.add(res);const timer=setInterval(()=>{try{res.write(': heartbeat\\n\\n')}catch{}},25000);req.on('close',()=>{clearInterval(timer);liveClients.delete(res)});return}
if(req.method==='POST'&&u.pathname==='/api/sara/control'){const id=aisid(req,res);let b='';for await(const c of req)b+=c;let x={};try{x=JSON.parse(b||'{}')}catch{return json(res,400,{error:'Invalid JSON'})}if(x.action==='stop')stopSara(id);else if(x.action==='resume')resumeSara(id);else return json(res,400,{error:'action must be stop or resume'});return json(res,200,{ok:true,stopped:getQueue(id).stopped})}
if(req.method==='POST'&&u.pathname==='/api/sara/event'){if(process.env.SARA_EVENT_INGEST_KEY&&req.headers['x-sara-event-key']!==process.env.SARA_EVENT_INGEST_KEY)return json(res,401,{error:'Invalid event ingest key'});const id=aisid(req,res);let b='';for await(const c of req)b+=c;let x={};try{x=JSON.parse(b||'{}')}catch{return json(res,400,{error:'Invalid JSON'})}const type=safeText(x.type,40);if(!['comment','gift','like','follow','share','system'].includes(type))return json(res,400,{error:'Unsupported event type'});const event={type,sessionId:id,viewerId:viewerKey(x),text:safeText(x.text,500),giftName:safeText(x.giftName,120),giftCount:Math.max(0,Number(x.giftCount)||0),source:safeText(x.source,80)||'external',created:Date.now()};emitLiveEvent(event);return json(res,202,{ok:true,event})}
if(req.method==='POST'&&u.pathname==='/api/sara/message')return saraMessage(req,res);
if(req.method==='POST'&&u.pathname==='/api/liveavatar/token'){try{return json(res,200,{ok:true,...await liveAvatarToken(req)})}catch(e){return json(res,502,{error:e.message})}}
if(u.pathname.startsWith('/api/sara/audio/')){const id=u.pathname.split('/').pop(),item=audioStore.get(id);if(!item)return send(res,404,'text/plain','Audio expired or not found');res.writeHead(200,{'Content-Type':'audio/mpeg','Cache-Control':'private, max-age=300'});return res.end(item.audio)}
if(u.pathname==='/auth/tiktok'){if(!KEY||!SECRET)return send(res,503,'text/plain','TikTok is not configured. Add TIKTOK_CLIENT_KEY and TIKTOK_CLIENT_SECRET in Vercel.');const state=oauthState(),redirect=tiktokRedirect(req);const q=new URLSearchParams({client_key:KEY,response_type:'code',scope:SCOPES,redirect_uri:redirect,state});res.writeHead(302,{Location:'https://www.tiktok.com/v2/auth/authorize/?'+q});return res.end()}
if(u.pathname==='/auth/tiktok/callback'){const state=u.searchParams.get('state'),code=u.searchParams.get('code'),error=u.searchParams.get('error'),errorDescription=u.searchParams.get('error_description');if(error)return send(res,400,'text/plain',`TikTok authorization error: ${error}${errorDescription?` — ${errorDescription}`:''}`);if(!validOAuthState(state)||!code)return send(res,400,'text/plain','TikTok authorization state is invalid or expired. Please try Connect TikTok again.');try{const redirect=tiktokRedirect(req);const form=new URLSearchParams({client_key:KEY,client_secret:SECRET,code,grant_type:'authorization_code',redirect_uri:redirect});const rr=await fetch('https://open.tiktokapis.com/v2/oauth/token/',{method:'POST',headers:{'Content-Type':'application/x-www-form-urlencoded','Cache-Control':'no-cache'},body:form}),d=await rr.json();if(!rr.ok||d.error)throw Error(d.error_description||d.error||'Token exchange failed');d.access_token_expires_at=Date.now()+Number(d.expires_in||86400)*1000;d.refresh_token_expires_at=Date.now()+Number(d.refresh_expires_in||31536000)*1000;const id=crypto.randomBytes(32).toString('hex');sessions.set(id,{tiktok:d,created:Date.now()});const secure=String(req.headers['x-forwarded-proto']||'').split(',')[0].trim()==='https';tokenCookie(res,id,secure);res.writeHead(302,{Location:'/?connected=1'});return res.end()}catch(e){return send(res,502,'text/plain','TikTok connection failed: '+e.message)}}
if(req.method==='POST'&&u.pathname==='/api/tiktok/webhook'){let b='';for await(const c of req)b+=c;let x={};try{x=JSON.parse(b)}catch{return json(res,400,{error:'Invalid JSON'})}if(KEY&&x.client_key&&x.client_key!==KEY)return json(res,403,{error:'Invalid client_key'});pruneWebhookEvents();const id=webhookId(x);if(webhookEvents.has(id))return json(res,200,{ok:true,duplicate:true});webhookEvents.set(id,Date.now());const event=String(x.event||'');let content={};try{content=typeof x.content==='string'?JSON.parse(x.content||'{}'):(x.content||{})}catch{}if(event==='authorization.removed'){for(const[sid,s]of sessions){if(s?.tiktok?.open_id===x.user_openid||s?.tiktok?.open_id===content?.open_id)sessions.delete(sid)}}return json(res,200,{ok:true,event,handled:event==='authorization.removed',liveInteractionEvent:false})}
if(req.method==='POST'&&u.pathname==='/api/tiktok/creator-info'){const s=session(req);if(!s)return json(res,401,{error:'TikTok account is not connected'});try{return json(res,200,await tiktok('/v2/post/publish/creator_info/query/','POST',await accessToken(s),{}))}catch(e){return json(res,502,{error:e.message})}}
if(req.method==='POST'&&u.pathname==='/api/tiktok/post-url'){const s=session(req);if(!s)return json(res,401,{error:'TikTok account is not connected'});let b='';for await(const c of req)b+=c;let x={};try{x=JSON.parse(b)}catch{}const videoUrl=String(x.videoUrl||'').trim();const title=String(x.title||'').trim().slice(0,2200);if(!videoUrl)return json(res,400,{error:'videoUrl required'});try{const body={post_info:{title,privacy_level:String(x.privacyLevel||'SELF_ONLY'),disable_duet:!!x.disableDuet,disable_comment:!!x.disableComment,disable_stitch:!!x.disableStitch,is_aigc:true},source_info:{source:'PULL_FROM_URL',video_url:videoUrl}};const result=await tiktok('/v2/post/publish/video/init/','POST',await accessToken(s),body);return json(res,200,{ok:true,publishId:result.data?.publish_id||null,result})}catch(e){return json(res,502,{error:e.message})}}
if(req.method==='POST'&&u.pathname==='/api/tiktok/post-status'){const s=session(req);if(!s)return json(res,401,{error:'TikTok account is not connected'});let b='';for await(const c of req)b+=c;let x={};try{x=JSON.parse(b)}catch{}const publishId=String(x.publishId||'').trim();if(!publishId)return json(res,400,{error:'publishId required'});try{return json(res,200,await tiktok('/v2/post/publish/status/fetch/','POST',await accessToken(s),{publish_id:publishId}))}catch(e){return json(res,502,{error:e.message})}}
if(req.method==='POST'&&u.pathname==='/api/ai/reply'){if(!AI_CONFIGURED)return json(res,503,{error:'AI is not configured. Add OPENAI_API_KEY.'});let b='';for await(const c of req)b+=c;let x={};try{x=JSON.parse(b)}catch{return json(res,400,{error:'Invalid JSON'})}try{const history=Array.isArray(x.messages)?x.messages.slice(-12).map(m=>({role:m.role==='assistant'?'assistant':'user',content:String(m.content||'').slice(0,2000)})):[];return json(res,200,{reply:await askAI(history,'ur')})}catch(e){return json(res,502,{error:e.message})}}
if(req.method==='POST'&&u.pathname==='/api/voice'){if(!VOICE_CONFIGURED)return json(res,503,{error:'Voice is not configured'});let b='';for await(const c of req)b+=c;let x={};try{x=JSON.parse(b)}catch{}try{const url=await makeVoice(String(x.text||'').slice(0,1200),'ur');return json(res,200,{ok:true,url})}catch(e){return json(res,502,{error:e.message})}}
if(u.pathname==='/'||u.pathname==='/index.html'){const html=fs.readFileSync(path.join(__dirname,'public','index.html'),'utf8');return send(res,200,'text/html; charset=utf-8',html)}
if(u.pathname==='/runtime.js'){const js=fs.readFileSync(path.join(__dirname,'runtime.js'),'utf8');return send(res,200,'application/javascript; charset=utf-8',js)}
return send(res,404,'text/plain','Not found')});
server.listen(PORT,()=>console.log(`SARA AI LIVE listening on ${PORT}`));
